# DSFlix Web Demo

This folder contains a simple HTML/CSS starter for the DSFlix streaming API.

- `index.html` — demo landing page with a sample fetch snippet
- `styles.css` — dark UI style for the demo page

Open `index.html` in a browser to preview the design.
